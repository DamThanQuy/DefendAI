# Dev Notes — Lệnh thường dùng

## Frontend (Next.js)
```powershell
cd DefendAI/apps/web; npm run dev
```

## Backend (FastAPI)
```powershell
cd DefendAI/apps/api; uvicorn app.main:app --reload
```

## Docker
```bash
cd DefendAI
docker-compose up -d
```

## Git
```bash
git add .
git commit -m "message"
git push
```
